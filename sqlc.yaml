version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "db"
        out: "db"
        # ここでUUIDの型を指定
        overrides:
          - db_type: "uuid"
            go_type: "github.com/google/uuid.UUID"